@page "/MyTests/AllTests"

@code {
    // `AllTests?filter=foo`      run tests that match "foo"
    // `AllTests?filter=%23myTag` run tests tagged with "myTag"
    // `AllTests?filter=$sup`     run IcuTestSuites that match "sup"
}

<IcuTestViewer Width="1000" />


<br/>
<br/>
<br/>
